# All optional. Without these, the contact form still works — messages are
# just logged (visible in Vercel's dashboard under your project's Logs)
# instead of emailed. Get a key at https://resend.com if you want emails.
RESEND_API_KEY=
CONTACT_TO_EMAIL=
CONTACT_FROM_EMAIL=
