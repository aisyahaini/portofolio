Drop project screenshots here, named to match each project's `image` path in
`server/src/data/seed.json` (e.g. `hr-inventory.png`). Until a file exists at
that path, the project card falls back to a themed placeholder automatically.

Suggested size: 1200×675 (16:9), PNG or JPG, under ~300KB.
